let usernames: string[] =[];
if (usernames.length === 0) {
    console.log("We need to find some users!");
} else {
    // Great users
}
 // Removing all usernames ensure the message "We need to find some users! is printed."